# Mind Influencer Reviews Backend — Python dependencies
# Install with:  pip install -r requirements.txt

Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Cors==4.0.1
PyJWT==2.8.0
Werkzeug==3.0.3

# Production server (optional for local dev, needed for deployment)
gunicorn==22.0.0

# If you upgrade to PostgreSQL later, add:
# psycopg2-binary==2.9.9
